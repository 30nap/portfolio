# Canonical origin of the deployed site (no trailing slash).
# On Vercel it falls back to the production domain automatically.
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Sub-path the site is served from, e.g. /portfolio. Empty for a root or custom domain.
# Set automatically by the GitHub Pages workflow.
NEXT_PUBLIC_BASE_PATH=
